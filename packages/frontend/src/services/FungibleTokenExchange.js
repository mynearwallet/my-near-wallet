class FungibleTokenExchange {
    #exchange;

    constructor() {}

    static async estimateSwap({
        senderId,
        receiverId,
        tokenIn,
        tokenInAmount,
        tokenOut,
    }) {}

    static async swap({
        senderId,
        receiverId,
        tokenIn,
        tokenInAmount,
        tokenOut,
    }) {}
}

export default new FungibleTokenExchange();
