import React from 'react';
import styled from 'styled-components';

const SettingsWrapper = styled.div``;

export default function SwapSettings() {
    return (
        <SettingsWrapper>
            <p>settings: slippage...</p>
        </SettingsWrapper>
    );
}
